
Docker Setup

1. Install Docker Desktop
2. Place backend folder and frontend folder alongside docker-compose.yml
3. Run:
   docker-compose up --build
4. Access:
   Frontend: http://localhost:3000
   Backend: http://localhost:5000
